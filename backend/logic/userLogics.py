from backend.api.models import User


def Calculate_over_all_rate(destination_user, rate):
    # calculate the overall rate of the user
    ...


def Save_rate(source_user, destination_user, rate):
    # save the rate of the user
    ...
